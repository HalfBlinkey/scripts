this script takes the process ID of a node server and its typically
memory usage. While the script is executing it effectively
**allows the server to sleep for a few second/ before restarting it
**checks the memory current memory usage/ if its to much restarts the server


to run this script on linux/mac
**
  deployment.sh <server.js> [extra params]
**
